name: "r3: list reference sources"
command: '"$(git rev-parse --show-toplevel)/.agents/scripts/sources.ps1" list'
description: List all registered r3 reference sources with branch, shallow flag, and store path.
tags:
    - r3
    - sources
